- resource ||= current_user
- following = resource.following
- followers = resource.followers
%h1
=resource
=t('users.connections')
.block
  %h3=t('users.following')
  .content
    - unless following.empty?
      - following.each do |user|
        %p= link_to user.name, user
    - else
      %p=t('users.not_following')
    
.block
  %h3=t('users.followers')
  .content
    - unless followers.empty?
      - followers.each do |user|
        %p= link_to user.name, user
    - else
      %p=t('users.no_followers')
