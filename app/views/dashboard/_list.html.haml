%ul.list.updates
  - feed.each do |update|
    - unless update.target.blank?
      %li
        .left= render_update_creator(update)
        .item
          %p
            =link_to update.creator.name,update.creator
            =render_update_content(update)