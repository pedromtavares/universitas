%h3.no-top
  =link_to t('groups.singular'), 'javascript:void(0)', :onclick => "$('#edit').slideUp(); $('#group_show').slideDown()"
  \->
  =link_to t('forums.plural'), group_forums_path(parent), :remote => true
  \->
  =link_to resource, resource_url, :remote => true
  \->
  = t('forums.edit')
= simple_form_for(resource, :url => resource_url, :html => { :class => 'form grid_6', :style => 'margin-left: -70px'}, :remote => true) do |f|
  = render :partial => "form", :locals => {:f => f}