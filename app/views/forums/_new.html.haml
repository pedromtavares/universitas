%h3.no-top
  =link_to t('groups.singular'), 'javascript:void(0)', :onclick => "$('#new').slideUp(); $('#group_show').slideDown()", :class => 'custom-history', :'data-history' => group_path(parent)
  \->
  =link_to t('forums.plural'), group_forums_path(parent), :remote => true, :class => 'history'
  \->
  = t('forums.new')
= simple_form_for(resource, :url => collection_url, :html => { :method => :post, :class => 'form grid_6', :style => 'margin-left: -70px'}, :remote => true) do |f|
  = render :partial => "form", :locals => {:f => f}
      