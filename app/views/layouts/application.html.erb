<!DOCTYPE html>
<html>
<head>
  <title>Universitas</title>
	  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/default/style", "web-app-theme/override", :cache => true %>
	<%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/">Universitas</a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
          <% if user_signed_in?%>
            <li><%= link_to t("shared.profile"), profile_path(current_user)%></li>
			      <li>Logged in as <%=current_user%>
			      <li><%= link_to t("shared.logout"), destroy_user_session_path, :class => "logout" %></li>
          <% else %>
            <li><%= link_to t("shared.login"), new_user_session_path, :class => "login" %></li>
          <% end %>
        </ul>
      </div>      
      <div id="main-navigation">
        <ul class="wat-cf">
        	<% %w{dashboard users courses}.each do |type|%>
				<% if controller_name == type%>
					<li class="active">
				<% else %>
					<li>
				<% end %>
				<%=link_to type.titleize, url_for(send(type+"_path"))%>
				</li>
			<%end%>
        </ul>
      </div>
    </div>    
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p><%=t('shared.copyright')%>&copy; <%= Time.now.year %> Universitas</p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>    
  </div>
<%= javascript_include_tag 'jquery/jquery-1.5.min' %>
<%= javascript_include_tag :all %>
</body>
</html>
  